import { Answer } from "../../types/answer"

export const trueAnswer: Answer = {
	title: "Quiz anwser",
	isTrue: true,
	type: "TEXT",
	fileLocation: null,
}

export const falseAnswer: Answer = {
	title: "Quiz anwser",
	isTrue: false,
	type: "TEXT",
	fileLocation: null,
}
